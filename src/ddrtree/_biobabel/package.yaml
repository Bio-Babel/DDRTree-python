# Generated by `biobabel new contract`.
# Fields tagged TODO must be filled before this contract is useful.
# Run `biobabel validate package --pkg ddrtree --strict` after editing.
schema_version: 1
repo: 'https://github.com/Bio-Babel/DDRTree-python'
distribution: ddrtree-python
import_name: ddrtree
display_name: DDRTree
contract_class: analysis
tier: 3
maturity: alpha
r_package:
  package: DDRTree
  repo: 'https://cran.r-project.org/package=DDRTree'
  version_or_commit: '0.1.6'
  fidelity: full
capabilities:
  - dimensionality_reduction
  - principal_graph_learning
domain_tags:
  - single-cell
  - trajectory-inference
task_tags:
  - pseudotime
  - reversed-graph-embedding
  - principal-graph
foundation: []
triggers:
  - "reduce a D x N data matrix to a low-dimensional embedding while jointly learning an explicit principal tree graph over the data (not just an embedding alone)"
  - "obtain a soft clustering of samples onto principal-graph nodes together with a minimum-spanning-tree topology connecting those nodes, e.g. as the dimensionality-reduction step feeding a downstream pseudotime/trajectory pipeline (the algorithm underlying Monocle 2)"
  - "need a numerically faithful Python port of the R DDRTree package (reversed graph embedding, Mao et al. KDD'15) for parity testing against R results"
not_when:
  - "only a plain linear or non-linear embedding is needed with no explicit tree/graph structure over the reduced points — use PCA, UMAP, or t-SNE instead"
  - "raw single-cell counts need normalization, QC, or feature selection first — ddrtree.DDRTree takes an already-preprocessed numeric (D, N) matrix, it does not read AnnData or perform preprocessing"
  - "GPU execution is required via backend='torch' but PyTorch is not installed — install the optional torch extra first"
